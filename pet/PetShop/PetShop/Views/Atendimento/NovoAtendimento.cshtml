
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="col-md-12">
    <div class="form-group">
        <label>Cliente Selecionado</label>
        <input hidden id="cod" value="@ViewBag.Id" />
        <input class="form-control" id="nome" readonly value="@ViewBag.NomeCliente" />
    </div>
    <div class="form-group">
        <label>Data do Atendimento*</label>
        <input type="date" class="form-control" id="data" />
    </div>
    <div class="form-group">
        <label>Selecione o Animal*</label>
        <select class="form-control" id="animal">
            <option value="">Selecione</option>
            @foreach (var item in ViewBag.Animais)
            {
                <option value="@item.Id">@item.Nome</option>
            }
        </select>
    </div>
    <div class="form-group">
        <label>Selecione o Funcionário*</label>
        <select class="form-control" id="funcionario">
            <option value="">Selecione</option>
            @foreach (var item in ViewBag.Funcionarios)
            {
                <option value="@item.IdFuncionario">@item.Nome</option>
            }
        </select>
    </div>
    <div class="form-group">
        <label>Valor*</label>
        <input class="form-control" id="valor" />
    </div>
    <div class="form-group">
        <label>Descrição do Atendimento</label>
        <textarea class="form-control" placeholder="Digite aqui..." id="descricao"></textarea>
    </div>
</div>
<button class="btn btn-success" onclick="NovoAtendimento()">Finalizar Atendimento</button>
<script src="~/ajax/geralajx.js"></script>
<script src="~/ajax/atendimento.js"></script>